using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Alb : Figur
{
    // Start is called before the first frame update
    public Alb()
    {
        setFigurName("Alb");
        setWerte(5, 2, 7);
        setBewegungRadius(7);
        setActionRadius(5);

    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
